# PhishGuard AI - runtime dependencies
# Python 3.11+ recommended (tested on 3.12).

Flask>=3.0,<4.0
scikit-learn>=1.4
numpy>=1.26
pandas>=2.0
joblib>=1.3
gunicorn>=21.0

# PostgreSQL driver — used automatically when POSTGRES_URL is set (Vercel).
# Falls back to built-in sqlite3 for local development.
psycopg2-binary>=2.9
